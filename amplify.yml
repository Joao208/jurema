backend:
  phases:
    build:
      commands:
        - '# Execute Amplify CLI with the helper script'
        - amplify push
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
    build:
      commands:
        - yarn run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*